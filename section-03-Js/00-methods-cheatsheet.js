Math.sqrt() es una función para calcular la raíz cuadrada de un número.

ParseFloat()

prompt()

alert() //este esta en el Function With Two Params & Return Statement
    .split()

String.length: The.length property of a string returns the number of characters that make up the string

Math.floor()
The Math.floor() function returns the largest integer less than or equal to the given number.

    dom
    La propiedad Element.innerHTML devuelve o establece la sintaxis HTML describiendo los descendientes del elemento.
    

REDONDEAR_
Math.ceil(7.3(ejemplo)) =>redondea a la alza
Math.floor(redondea a un numero a la baja)
Math.round(redondea al entero más cercano)



Math.sqrt(RAIZ CUADRADA DE UN numero)
Math.pow(elevar potencia)
Math.sign(indicador de si el num es positivo(=1)cero(0)o negativo(-1)siempre da esos valores segun si es negativo positivo o 0)
Math.random(siempre da un numero aleatorio entre cero y uno)
Math.random() * 1000 = te da numero aleatorio entre el 0 y el mil 

por ejemplo "un sorteo" queremos un numero aleatorio entre el 0 y el 1000
Math.round(Math.random()*1000) así saldía un numero añeatorio entero entre el 0 y el 1000